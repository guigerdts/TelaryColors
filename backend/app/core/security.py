"""Authentication primitives (bcrypt hashing + JWT).

Placeholder for slice A — implemented in the auth slice (Phase 3):
bcrypt direct hashing with the 72-byte guard, JWT HS256 encode/decode
with a 12-hour expiry. The module exists so the core package layout is
stable from day one.
"""